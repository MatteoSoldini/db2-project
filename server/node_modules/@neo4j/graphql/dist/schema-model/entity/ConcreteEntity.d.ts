import type { Annotation, Annotations } from "@neo4j/graphql/src/schema-model/annotation/Annotation";
import type { Attribute } from "@neo4j/graphql/src/schema-model/attribute/Attribute";
import type { Entity } from "@neo4j/graphql/src/schema-model/entity/Entity";
export declare class ConcreteEntity implements Entity {
    readonly name: string;
    readonly labels: Set<string>;
    readonly attributes: Map<string, Attribute>;
    readonly annotations: Partial<Annotations>;
    constructor({ name, labels, attributes, annotations, }: {
        name: string;
        labels: string[];
        attributes?: Attribute[];
        annotations?: Annotation[];
    });
    matchLabels(labels: string[]): boolean;
    private addAttribute;
    private addAnnotation;
    private setsAreEqual;
}
//# sourceMappingURL=ConcreteEntity.d.ts.map